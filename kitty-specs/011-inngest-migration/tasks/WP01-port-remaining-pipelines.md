# WP01 — Port Remaining Pipeline Functions

Port `content-audit-pipeline` and `regulatory-change-monitor-pipeline` to Inngest
functions, register them in `allFunctions`, and add unit tests.

## Subtasks

- T001: Create `content-audit-pipeline.ts` following `corpus-update-pipeline.ts` pattern
- T002: Create `regulatory-change-monitor-pipeline.ts` following the same pattern
- T003: Register both in `allFunctions` in `inngest/index.ts`
- T004: Write unit tests (vitest) for both functions

## Status

Complete — merged to main.
