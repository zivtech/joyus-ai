# OS
.DS_Store

# Private staging directories (repo separation)
_private/
.entire/
.conductor/
.mcp.json

# AI Agent directories (added by Spec Kitty CLI)
# These contain auth tokens and should NEVER be committed
.claude/
.codex/
.opencode/
.windsurf/
.gemini/
.cursor/
.qwen/
.kilocode/
.augment/
.roo/
.amazonq/
.github/copilot/
.omc/
# Block WP status files (managed in main repo, prevents merge conflicts)
# Research artifacts in kitty-specs/**/research/ are allowed
kitty-specs/**/tasks/*.md

# Deploy secrets (never commit actual .env files)
deploy/.env
