# joyus-profile-engine

Writing profile engine for the Joyus AI platform. Analyzes document corpora to build author profiles, performs attribution verification, generates voice-faithful content, and monitors fidelity drift.

## Installation

```bash
uv pip install -e ".[dev]"
```

## Usage

```bash
joyus-profile --help
```
